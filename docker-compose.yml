services:
  frontend:
    build:
      context: .
      args:
        VITE_BACK_URL: ${VITE_BACK_URL}
        VITE_CALLBACK_URL: ${VITE_CALLBACK_URL}
    ports:
      - '80:80'
    restart: unless-stopped
